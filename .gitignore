node_modules/
.DS_Store
*.log

# Vrais secrets futurs (clé de service Firebase Admin, etc.)
# — jamais commités, même par erreur
.env
.env.*
!.env.example

# Sources brutes non compressées (ex: exports IA type Gemini/Midjourney)
# À traiter (compression + renommage) AVANT tout commit dans assets/img/
Gemini_Generated_Image_*
*_raw.*
*_original.*
